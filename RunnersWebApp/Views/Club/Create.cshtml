@using RunnersWebApp.Data.Enum
@using RunnersWebApp.ViewModels
@model CreateClubViewModel

@{
    ViewBag.Title = "Create Club";
}

<div class="container mt-5">
    <h2 class="text-center mb-4">Create a New Club</h2>

    <form method="post" asp-action="Create" enctype="multipart/form-data" class="card p-4 shadow-sm">
        <input type="hidden" id="AppUserId" asp-for="AppUserId" />

        <!-- Title -->
        <div class="mb-3">
            <label asp-for="Title" class="form-label fw-bold"></label>
            <input asp-for="Title" class="form-control" placeholder="Enter club title" />
            <span asp-validation-for="Title" class="text-danger small"></span>
        </div>

        <!-- Description -->
        <div class="mb-3">
            <label asp-for="Description" class="form-label fw-bold"></label>
            <textarea asp-for="Description" class="form-control" rows="4" placeholder="Enter club description"></textarea>
            <span asp-validation-for="Description" class="text-danger small"></span>
        </div>

        <!-- Club Category -->
        <div class="mb-3">
            <label asp-for="ClubCategory" class="form-label fw-bold"></label>
            <select asp-for="ClubCategory" asp-items="@Html.GetEnumSelectList<ClubCategory>()" class="form-select">
                <option>--Select--</option>
            </select>
            <span asp-validation-for="ClubCategory" class="text-danger small"></span>
        </div>

        <!-- Address Section -->
        <div class="mb-3">
            <h5 class="fw-bold mb-3">Address Details</h5>

            <!-- City -->
            <div class="mb-3">
                <label asp-for="Address.City" class="form-label fw-bold"></label>
                <input asp-for="Address.City" class="form-control" placeholder="Enter city" />
                <span asp-validation-for="Address.City" class="text-danger small"></span>
            </div>

            <!-- Country -->
            <div class="mb-3">
                <label asp-for="Address.Country" class="form-label fw-bold"></label>
                <input asp-for="Address.Country" class="form-control" placeholder="Enter country" />
                <span asp-validation-for="Address.Country" class="text-danger small"></span>
            </div>

            <!-- Street -->
            <div class="mb-3">
                <label asp-for="Address.Street" class="form-label fw-bold"></label>
                <input asp-for="Address.Street" class="form-control" placeholder="Enter street" />
                <span asp-validation-for="Address.Street" class="text-danger small"></span>
            </div>
        </div>

        <!-- Image Upload -->
        <div class="mb-4">
            <label asp-for="Image" class="form-label fw-bold"></label>
            <input asp-for="Image" type="file" class="form-control" />
            <span asp-validation-for="Image" class="text-danger small"></span>
        </div>

        <!-- Submit Button -->
        <div class="d-grid">
            <button type="submit" class="btn btn-primary btn-lg">Create Club</button>
        </div>
    </form>
</div>